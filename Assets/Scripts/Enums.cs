public enum Resource{seedling, mana, blood};

public enum Stat{maxHealth, castSpeed, spellPower, moveSpeed};

public enum Flowers{sunflower, shrubbery};

public enum Spells{wrath, mudPool};

public enum SpellType{projectile, groundPlaced};

public enum FlowerSpriteType
{
    middle,
    horizontal,
    vertical,
    up, 
    down, 
    left, 
    right, 
    upLeftCorner, 
    upRightCorner, 
    downLeftCorner, 
    downRightCorner,
};